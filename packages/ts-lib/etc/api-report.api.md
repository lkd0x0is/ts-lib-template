## API Report File for "@lkd0x0is/ts-lib-example"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export class Calculator {
    constructor(options?: CalculatorOptions);
    // @beta
    add(operand: number): number;
    clear(): this;
    get currentValue(): number;
    // @alpha
    divide(operand: number): number;
    executeOperation(operationName: string, operand: number): number;
    get history(): readonly HistoryEntry[];
    // @internal
    multiply(operand: number): number;
    get precision(): number;
    set precision(value: number);
    registerOperation(name: string, operation: Omit<NamedOperation, "name">): void;
    setValue(value: number): this;
    subtract(operand: number): number;
}

// @public
export interface CalculatorOptions {
    initialValue?: number;
    precision?: number;
}

// @public
export interface HistoryEntry {
    operands: [number, number?];
    operationName: string;
    valueAfter: number;
    valueBefore: number;
}

// @public
export interface NamedOperation extends Operation {
    name: string;
}

// @public
export interface Operation {
    execute: (currentValue: number, operand: number) => number;
    symbol: string;
}

```
